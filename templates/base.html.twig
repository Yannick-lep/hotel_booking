<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Réservation Hôtel{% endblock %}</title>
        
        {# Inclusion de Bootstrap pour le style #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        
        {# Block pour ajouter du CSS supplémentaire dans les pages enfants #}
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {# BARRE DE NAVIGATION #}
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                {# Logo / Nom du site #}
                <a class="navbar-brand" href="{{ path('home') }}"> Hôtel Services</a>
                
                {# Bouton hamburger pour mobile #}
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                        </li>
                        
                        {# Affichage conditionnel selon l'état de connexion #}
                        {% if app.user %}
                            {# Si connecté #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('reservation_list') }}">Mes réservations</a>
                            </li>
                            <li class="nav-item">
                                <span class="nav-link">👤 {{ app.user.email }}</span>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                            </li>
                        {% else %}
                            {# Si non connecté #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>

        {# AFFICHAGE DES MESSAGES FLASH #}
        <div class="container mt-3">
            {# Les messages flash sont temporaires (affichés une seule fois) #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

        {# CONTENU PRINCIPAL #}
        {# C'est ici que le contenu des pages enfants sera injecté #}
        <main class="container my-4">
            {% block body %}{% endblock %}
        </main>

        {# PIED DE PAGE #}
        <footer class="bg-light text-center py-3 mt-5">
            <div class="container">
                <p class="mb-0">&copy; 2026 Hôtel Services - Tous droits réservés</p>
            </div>
        </footer>

        {# JavaScript Bootstrap #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        
        {% block javascripts %}{% endblock %}
    </body>
</html>